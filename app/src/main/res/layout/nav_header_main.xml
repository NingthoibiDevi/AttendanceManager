<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:background="#2D692F">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="70dp"
        android:layout_height="58dp"
        android:contentDescription="Teacher's Profile"
        android:paddingTop="8dp"
        app:srcCompat="@drawable/attend3" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:textSize="20dp"
        android:id="@+id/nm1"
        android:text="Teacher's Profile"
        android:textStyle="bold"
        android:textAppearance="@style/ThemeOverlay.AppCompat.Dark"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/eid"
        android:textSize="20dp"
        android:text="My Register"
        android:textStyle="bold" />

</LinearLayout>